<div class="row">
  <% @coffee_shops.each do |shop| %>
    <div class="col-md-4 mb-4">
      <div class="card">
        <div class="card-body">
          <h5 class="card-title"><%= shop.name %></h5>
          <p class="card-text">
            <%= shop.city %>, <%= shop.country %>
            <br>Rating: <%= shop.average_rating %>
          </p>
          <%= link_to "View Details", coffee_shop_path(shop), class: "btn btn-primary" %>
        </div>
      </div>
    </div>
  <% end %>
</div>